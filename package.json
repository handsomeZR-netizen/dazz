{
  "name": "dazz-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Dazz Web — film-style camera filter for OPPO Find X6",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host",
    "test:fixtures": "node scripts/make-fixtures.mjs",
    "test:smoke": "npm run build && npm run test:fixtures && node scripts/smoke.mjs"
  },
  "devDependencies": {
    "playwright": "^1.59.1",
    "vite": "^5.4.10",
    "vite-plugin-mkcert": "^1.17.6",
    "vite-plugin-pwa": "^1.2.0"
  }
}
